const gameIds = {
    writing: 'writing',
    audio: 'audio',
    definitionWord: 'definitionWord'
};

const config = {
    MAX_INDEX: 5,
    MAX_GAMES: 5,
}

exports.gameIds = gameIds;
exports.config = config;